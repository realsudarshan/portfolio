---
title: Understanding Node.js File Module
description: Explore the powerful Node.js File Module in this comprehensive guide. Learn how to work with files effectively, including reading, writing, updating, and deleting operations. Enhance your backend development skills with practical examples and clear explanations.
banner: /images/understanding-node-file-module/og.jpg
date: 2024-11-19
tags:
  - Node.js
  - JavaScript
  - Backend Development
---

Node.js provides the **File System (fs) module** to handle file operations such as reading, writing, updating, and deleting files. This built-in module makes managing files in a backend application both simple and efficient.

<Image 
  src="/images/understanding-node-file-module/hero.png" 
  alt="Node.js File System" 
/>

## Importing the File System Module

The `fs` module is included with Node.js, so there's no need for additional installation. Start by importing it into your application:
'''js
const fs=require('fs) 
import fs from 'fs'; //for echmascript
'''
##key feature of fsmodule
1. Synchronous and Asynchronous Methods
2. Promises API

##Useful code snippets
1. Read file 
```js
//asynchronous
fs.readFile('example.txt','utf-08',(err,data)=>{
if (err) throw err;
console.log(data)
})
//synchronous
const data=fs.readFilesync('example.txt','utf-08')
console.log(data);
//using Promises
async function readFile(){
 try {
    const data = await fs.readFile('example.txt', 'utf8');
    console.log(data);
  } catch (err) {
    console.error(err);
  }
}
readFile();
```
2. Write in file 
```js
//async
fs.writeFile('example.txt', 'Hello, World!', (err) => {
  if (err) throw err;
  console.log('File written successfully!');
});
//sync
fs.writeFileSync('example.txt', 'Hello, World!');
```
3. Append and Deleting to a file
```js
//async append
fs.appendFile('example.txt', '\nThis is appended text.', (err) => {
  if (err) throw err;
  console.log('Data appended!');
});
//async delete
fs.unlink('example.txt', (err) => {
  if (err) throw err;
  console.log('File deleted!');
});
//sync delete
fs.unlinkSync('example.txt');

4.Renaming file
```js
//async
fs.rename('oldname.txt', 'newname.txt', (err) => {
  if (err) throw err;
  console.log('File renamed!');
});
```









